version: 1
frontend:
  phases:
    build:
      commands:
        - 'pip install -r requirements.txt'
      artifacts:
        baseDirectory: /
        files:
            - '**/*'
    cache:
        paths: []
